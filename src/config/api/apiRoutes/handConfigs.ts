export const HAND_CONFIG= {
    CREATE:   ()           => `hand-config`,
    FIND_ALL: ()           => `hand-config`,
    UPDATE:   (id: string) => `hand-config/${id}`,
    DELETE:   (id: string) => `hand-config/${id}`,
}
